import MaterialIcons from 'react-native-vector-icons/MaterialIcons';

export const iconOverrides = {
  baseStyle: {
    color: 'text.primary'
  },
  defaultProps: {
    as: MaterialIcons,
    size: 'xl'
  }
};
